{
    "$schema": "http://json-schema.org/draft-07/schema",
    "$id": "https://raw.githubusercontent.com/rujinlong/nf-phamseek/main/nextflow_schema.json",
    "title": "rujinlong/nf-phamseek pipeline parameters",
    "description": "Phage detection in low-biomass clinical Oxford Nanopore metagenomes.",
    "type": "object",
    "definitions": {
        "input_output_options": {
            "title": "Input/output options",
            "type": "object",
            "fa_icon": "fas fa-terminal",
            "required": [
                "input"
            ],
            "properties": {
                "input": {
                    "type": "string",
                    "format": "file-path",
                    "mimetype": "text/csv",
                    "description": "Samplesheet: sample_id,fastq[,platform][,sample_type].",
                    "help_text": "CSV with a header row. `sample_id` must be unique and free of whitespace. `fastq` may be absolute or relative to the samplesheet's own directory. `platform` defaults to `ont` (the only value implemented in v0.1). `sample_type` is one of `sample`, `ntc`, `positive_control`; taxa also seen in an `ntc` are flagged in the report, but never subtracted.",
                    "fa_icon": "fas fa-file-csv"
                },
                "outdir": {
                    "type": "string",
                    "format": "directory-path",
                    "description": "Output directory.",
                    "default": "results",
                    "fa_icon": "fas fa-folder-open"
                },
                "mode": {
                    "type": "string",
                    "default": "fast",
                    "enum": [
                        "fast",
                        "full"
                    ],
                    "description": "Analysis tier. Only `fast` is implemented in v0.1.",
                    "help_text": "`fast` is read-level: QC, kraken2, two-level host depletion, bracken, report. `full` would add assembly (flye), geNomad and CheckV; it is deliberately not wired up in v0.1 and fails immediately with an explanation rather than running a partial path.",
                    "fa_icon": "fas fa-layer-group"
                }
            }
        },
        "database_options": {
            "title": "Reference databases",
            "type": "object",
            "fa_icon": "fas fa-database",
            "description": "Databases are always external to the repository and are never copied into the work directory.",
            "properties": {
                "db_dir": {
                    "type": "string",
                    "format": "directory-path",
                    "description": "Database root. Expects <db_dir>/kraken2 and <db_dir>/host.",
                    "fa_icon": "fas fa-folder"
                },
                "kraken2_db": {
                    "type": "string",
                    "format": "directory-path",
                    "description": "kraken2 database directory. Overrides <db_dir>/kraken2.",
                    "fa_icon": "fas fa-folder"
                },
                "host_index": {
                    "type": "string",
                    "format": "directory-path",
                    "description": "Directory holding the host reference (.mmi or FASTA). Overrides <db_dir>/host.",
                    "fa_icon": "fas fa-folder"
                },
                "db_has_decoy": {
                    "type": [
                        "string",
                        "boolean"
                    ],
                    "default": "auto",
                    "description": "Whether the kraken2 database contains non-phage decoy sequences: auto (default), true or false.",
                    "help_text": "`auto` (default) reads the database's own taxonomy with kraken2-inspect and reports the human, bacterial and plasmid decoy classes separately, so an unverifiable class is marked unknown rather than guessed. `true`/`false` override that with an operator declaration; when a declaration contradicts what the database actually contains, the report says so instead of reasoning from the wrong premise. This matters because it selects the false-positive caveat: without plasmid decoy sequences roughly 37% of plasmid-derived sequence is called phage, with them at or below 0.4%. Raising --kraken2_confidence does not fix that, because the signal is real homology (shared integrase and relaxase modules), not stray k-mers.",
                    "fa_icon": "fas fa-shield-alt"
                },
                "db_label": {
                    "type": "string",
                    "description": "Human-readable database name recorded in the report and manifest.",
                    "fa_icon": "fas fa-tag"
                }
            }
        },
        "qc_options": {
            "title": "Long-read QC",
            "type": "object",
            "fa_icon": "fas fa-filter",
            "properties": {
                "chopper_min_quality": {
                    "type": "integer",
                    "default": 10,
                    "description": "Minimum mean Phred quality per read (chopper -q).",
                    "fa_icon": "fas fa-check"
                },
                "chopper_min_length": {
                    "type": "integer",
                    "default": 200,
                    "description": "Minimum read length in bp (chopper -l).",
                    "fa_icon": "fas fa-ruler-horizontal"
                }
            }
        },
        "host_depletion_options": {
            "title": "Host depletion",
            "type": "object",
            "fa_icon": "fas fa-user-shield",
            "description": "Two levels. Level 1 deletes the host subtree using the kraken2 pass that has already been paid for; level 2 aligns the remainder against the host reference. A kraken2 decoy only relabels reads, so level 2 is what makes the deletion complete.",
            "properties": {
                "skip_host_removal": {
                    "type": "boolean",
                    "default": false,
                    "description": "Skip the level-2 aligner. Level 1 still runs.",
                    "help_text": "Leaves any host read the database does not recognise in the output. Intended for development and for databases with well-characterised host decoy content, not for data that leaves the institution.",
                    "fa_icon": "fas fa-forward"
                },
                "host_taxid": {
                    "type": "integer",
                    "default": 9606,
                    "description": "Root of the host subtree to delete at level 1.",
                    "fa_icon": "fas fa-dna"
                },
                "viral_taxid": {
                    "type": "integer",
                    "default": 10239,
                    "description": "Root of the viral subtree counted as evidence.",
                    "fa_icon": "fas fa-virus"
                },
                "l2_input": {
                    "type": "string",
                    "default": "all_nonhuman",
                    "enum": [
                        "all_nonhuman",
                        "nonhuman_nonviral"
                    ],
                    "description": "Which reads the level-2 aligner sees.",
                    "help_text": "`all_nonhuman` (default) sends every read outside the host subtree to the aligner, so a human read misclassified as viral is still caught. `nonhuman_nonviral` withholds viral-classified reads: marginally faster, but it opens exactly that gap.",
                    "fa_icon": "fas fa-code-branch"
                },
                "minimap2_preset": {
                    "type": "string",
                    "default": "map-ont",
                    "description": "minimap2 preset for level-2 alignment.",
                    "help_text": "Must match the preset the .mmi index was built with; minimap2 silently honours the index's parameters over the command line.",
                    "fa_icon": "fas fa-sliders-h"
                }
            }
        },
        "classification_options": {
            "title": "Classification and abundance",
            "type": "object",
            "fa_icon": "fas fa-sitemap",
            "properties": {
                "kraken2_confidence": {
                    "type": "number",
                    "default": 0.02,
                    "minimum": 0,
                    "maximum": 1,
                    "description": "kraken2 --confidence. Leave at 0.02 for ONT.",
                    "help_text": "0.02, not kraken2's default of 0. Against a phage reference database the default calls a taxon on a single k-mer hit, which made 61% of human and 38% of clean bacterial contigs look like phage in the benchmark this pipeline derives from. Do NOT raise it: short-read intuition does not transfer to ONT. Going from 0.02 to 0.10 costs only ~3.5 points of recall on 150 bp reads at 0.1% error, but 15 points at 95% read identity (typical R10.4 simplex) and 29 points at 87% identity, because each ONT read carries far fewer exactly-matching k-mers to vote with. Raising it also does not remove plasmid and ICE false positives, which come from real shared modules rather than stray k-mers.",
                    "fa_icon": "fas fa-percent"
                },
                "skip_bracken": {
                    "type": "boolean",
                    "default": true,
                    "description": "Skip bracken abundance re-estimation (default: skipped).",
                    "help_text": "Off by default on purpose: bracken re-estimates abundance from a k-mer distribution built for one fixed read length, an assumption ONT reads violate by construction. Kraken2 read counts and the RPM derived from them are reported regardless and do not depend on bracken. If you enable it, set bracken_read_length from the median read length of your own run -- there is no defensible default. Bracken is also skipped automatically, with a warning, when the database has no database<N>mers.kmer_distrib files.",
                    "fa_icon": "fas fa-forward"
                },
                "bracken_read_length": {
                    "type": [
                        "integer",
                        "null"
                    ],
                    "description": "Read length whose k-mer distribution bracken should use. Required when skip_bracken is false; no default, because ONT runs differ.",
                    "fa_icon": "fas fa-ruler"
                },
                "bracken_level": {
                    "type": "string",
                    "default": "S",
                    "enum": [
                        "D",
                        "P",
                        "C",
                        "O",
                        "F",
                        "G",
                        "S"
                    ],
                    "description": "Taxonomic rank for bracken re-estimation.",
                    "fa_icon": "fas fa-layer-group"
                },
                "bracken_threshold": {
                    "type": "integer",
                    "default": 10,
                    "description": "Minimum reads for bracken to re-estimate a taxon.",
                    "fa_icon": "fas fa-greater-than-equal"
                }
            }
        },
        "reporting_options": {
            "title": "Reporting thresholds",
            "type": "object",
            "fa_icon": "fas fa-file-medical-alt",
            "description": "v0.1 reports read-level evidence only, so every positive is a candidate. These thresholds decide how a candidate is graded, not whether it is confirmed.",
            "properties": {
                "min_reads": {
                    "type": "integer",
                    "default": 10,
                    "description": "Minimum reads for a taxon to be graded above `below_threshold`.",
                    "fa_icon": "fas fa-greater-than-equal"
                },
                "min_rpm": {
                    "type": "number",
                    "default": 1.0,
                    "description": "Minimum reads per million non-host reads for `candidate_passes_abundance_screen`.",
                    "help_text": "The denominator is non-host reads, not total reads: in plasma and CSF the human fraction dominates, and normalising by total reads would make the same organism look far rarer in a sample that simply had more host background.",
                    "fa_icon": "fas fa-percent"
                }
            }
        },
        "max_job_request_options": {
            "title": "Maximum job request options",
            "type": "object",
            "fa_icon": "fab fa-acquisitions-incorporated",
            "properties": {
                "max_cpus": {
                    "type": "integer",
                    "default": 16,
                    "description": "Maximum CPUs any single task may request.",
                    "fa_icon": "fas fa-microchip"
                },
                "max_memory": {
                    "type": "string",
                    "default": "128.GB",
                    "pattern": "^\\d+(\\.\\d+)?\\.?\\s*(K|M|G|T)?B$",
                    "description": "Maximum memory any single task may request.",
                    "help_text": "kraken2 loads the whole database into RAM, so this must exceed the database size. The 11 GB production database peaks around 12 GB.",
                    "fa_icon": "fas fa-memory"
                },
                "max_time": {
                    "type": "string",
                    "default": "240.h",
                    "pattern": "^(\\d+\\.?\\s*(s|m|h|d|day)\\s*)+$",
                    "description": "Maximum time any single task may request.",
                    "fa_icon": "far fa-clock"
                }
            }
        },
        "generic_options": {
            "title": "Generic options",
            "type": "object",
            "fa_icon": "fas fa-file-import",
            "properties": {
                "help": {
                    "type": "boolean",
                    "description": "Display help text.",
                    "fa_icon": "fas fa-question-circle",
                    "hidden": true
                },
                "version": {
                    "type": "boolean",
                    "description": "Display version and exit.",
                    "fa_icon": "fas fa-question-circle",
                    "hidden": true
                },
                "monochrome_logs": {
                    "type": "boolean",
                    "description": "Do not use colour in the log output.",
                    "fa_icon": "fas fa-palette",
                    "hidden": true
                },
                "validate_params": {
                    "type": "boolean",
                    "default": true,
                    "description": "Validate parameters against this schema at startup.",
                    "fa_icon": "fas fa-check-square",
                    "hidden": true
                },
                "validationFailUnrecognisedParams": {
                    "type": "boolean",
                    "description": "Fail when an unrecognised parameter is given.",
                    "fa_icon": "far fa-check-circle",
                    "hidden": true
                },
                "validationLenientMode": {
                    "type": "boolean",
                    "description": "Validate parameters in JSON Schema lenient mode.",
                    "fa_icon": "far fa-check-circle",
                    "hidden": true
                },
                "validationShowHiddenParams": {
                    "type": "boolean",
                    "description": "Show hidden parameters in --help.",
                    "fa_icon": "far fa-eye-slash",
                    "hidden": true
                },
                "pipeline_version": {
                    "type": "string",
                    "description": "Pipeline version. Not user-settable.",
                    "help_text": "Feeds both manifest.version and the container tag so the two cannot disagree. It lives in params because Nextflow's strict config parser allows no top-level `def`, making params the only namespace a value can cross config scopes through.",
                    "hidden": true
                },
                "trace_timestamp": {
                    "type": "string",
                    "description": "Timestamp shared by this run's four execution reports. Not user-settable.",
                    "hidden": true
                }
            }
        }
    },
    "allOf": [
        {
            "$ref": "#/definitions/input_output_options"
        },
        {
            "$ref": "#/definitions/database_options"
        },
        {
            "$ref": "#/definitions/qc_options"
        },
        {
            "$ref": "#/definitions/host_depletion_options"
        },
        {
            "$ref": "#/definitions/classification_options"
        },
        {
            "$ref": "#/definitions/reporting_options"
        },
        {
            "$ref": "#/definitions/max_job_request_options"
        },
        {
            "$ref": "#/definitions/generic_options"
        }
    ]
}
